# nvim

Active Neovim config is **not** in this dotfiles repo.

It lives here: [james-yap/kickstart.nvim](https://github.com/james-yap/kickstart.nvim)

```bash
git clone git@github.com:james-yap/kickstart.nvim.git ~/.config/nvim
```

Former LazyVim setup (not stowed): `archived/neovim-lazyvim/`
